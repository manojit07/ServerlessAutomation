version: 0.1

phases:
  install:
    commands:
      - echo inside install

  pre_build:
    commands:
      - echo inside pre_build

  build:
    commands:
      - echo inside build

  post_build:
    commands:
      - echo inside post_build

artifacts:
  filenames:
    -$Artifact_Name 